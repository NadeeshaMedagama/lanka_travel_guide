# Base URL of the REST API (json-server mock backend).
# Vite only exposes variables prefixed with VITE_ to client-side code.
# Copy this file to `.env` to override. If unset, the app falls back to http://localhost:3001.
VITE_API_URL=http://localhost:3001
